# Banana - ComfyUI Gemini Image Generator 依赖文件
#一般不需要单独装依赖
# 安装方法:
# pip install -r requirements.txt
#
# 注意: ComfyUI 自带的依赖库已注释掉,无需重复安装

# ============================================
# ComfyUI 一般自带的依赖 (无需安装)
# ============================================
# torch>=2.0.0              # PyTorch - ComfyUI 核心依赖
# numpy>=1.23.0             # NumPy - ComfyUI 核心依赖
# Pillow>=9.0.0             # PIL 图像处理库 - ComfyUI 核心依赖
# aiohttp>=3.8.0            # 异步 HTTP 客户端/服务器 - ComfyUI 核心依赖

# ============================================
# 本项目需要的额外依赖(装的节点比较多的话一般也自带了)
# ============================================
# requests - HTTP 请求库,用于调用 Gemini API
# 版本说明:
#   - 最低要求 2.20.0 (2018年发布,支持基本功能)
#   - 推荐 2.31.0+ (2023年发布,修复安全漏洞)
#   - 使用 >= 只要求最低版本,pip会优先使用已安装版本或最新稳定版
#   - 有使用其他api调用节点的话一般也会装了这个依赖
requests>=2.20.0



# ============================================
# 可选依赖 (用于提升性能)
# ============================================
# 以下依赖通常会随 requests 自动安装,无需手动指定:
# urllib3>=1.26.0           # requests 的核心依赖,自动安装
# certifi>=2022.0.0         # SSL 证书验证,requests 会自动安装
# charset-normalizer        # 字符编码检测,requests 会自动安装
# idna                      # 国际化域名支持,requests 会自动安装
